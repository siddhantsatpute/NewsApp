import 'package:flutter/material.dart';

import 'design/startup/startup_screen.dart';

///Main function to start the app
void main() {
  //runApp function to call the very first screen of the app.
  runApp(const StartupScreen());
}
